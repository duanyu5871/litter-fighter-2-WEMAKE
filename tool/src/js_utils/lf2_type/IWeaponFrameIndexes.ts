export interface IWeaponFrameIndexes {
  in_the_sky: string;
  // on_hand (在手中)
  throwing: string;
  on_ground: string;
  just_on_ground: string //(剛掉到地上的動作)
  // just_on_on_ground: string// (同上，有何分別我也不清楚)
}
