
export interface IFramePictureInfo {
  /** 纹理ID */
  tex: number | string;
  /** 精灵图裁剪起点X坐标（像素） */
  x: number;
  /** 精灵图裁剪起点Y坐标（像素） */
  y: number;
  /** 精灵图宽度（像素） */
  w: number;
  /** 精灵图高度（像素） */
  h: number;
}
